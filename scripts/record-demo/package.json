{
  "name": "@openflipbook/record-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright driver that records the demo clip used on the landing page.",
  "type": "module",
  "scripts": {
    "start": "tsx record.ts",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.48.0",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
